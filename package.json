{
  "name": "the-aspect",
  "version": "1.0.0",
  "private": true,
  "description": "The Aspect with server-side OpenRouter proxying for Render.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
